describe('cgross.soft-forms', function() {

  beforeEach(module('app'));

  var scope,compile;

  beforeEach(inject(function($rootScope,$compile) {
    scope = $rootScope.$new();
    compile = $compile;

  }));

  it('...', function() {

    //todo

  });

});